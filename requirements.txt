# Python dependencies for cesiumjs-skills evaluation framework
#
# LLM calls go through a local agent CLI harness (`opencode` by default, or
# `codex` when selected). Install the chosen CLI, ensure it is on PATH, and
# authenticate it. No provider-specific Python SDK is required.

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.2.0
pytest-mock>=3.14.0

# Browser automation for evaluation runner (US-006)
playwright>=1.40.0

# JSON schema validation
jsonschema>=4.21.0
