{
  "name": "cesiumjs-skills",
  "private": true,
  "description": "CesiumJS Agent Skills with a TypeScript evaluation and self-optimization platform.",
  "workspaces": [
    "packages/*",
    "apps/evaluation-console"
  ],
  "scripts": {
    "build": "npm run build --workspace @cesiumjs-skills/eval && npm run build --workspace @cesiumjs-skills/evaluation-console",
    "test": "npm test --workspace @cesiumjs-skills/eval",
    "eval": "node packages/eval/bin/cesium-eval.js"
  },
  "engines": {
    "node": ">=20"
  }
}
