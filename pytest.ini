[pytest]
# Put the repo root on sys.path so `import optimization` and `import evaluation`
# resolve under bare `pytest` as well as `python -m pytest`.
pythonpath = .
testpaths =
    harness/tests
    evaluation/tests
    optimization/tests
